{
  "private": true,
  "scripts": {
    "test": "npm -s run lint",
    "lint": "npm -s run lint:prettier",
    "lint:prettier": "prettier '**/*.{js,json,md,yml,xml}' --check",
    "fix": "prettier '**/*.{js,json,md,yml,xml}' --write"
  },
  "devDependencies": {
    "@prettier/plugin-xml": "^0.13.1",
    "prettier": "^2.3.1"
  }
}
