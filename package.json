{
  "private": true,
  "scripts": {
    "test": "npm -s run lint",
    "lint": "npm -s run lint:prettier",
    "lint:prettier": "prettier . --check",
    "fix": "prettier . --write"
  },
  "devDependencies": {
    "@prettier/plugin-xml": "^2.0.1",
    "prettier": "2.6.2"
  }
}
