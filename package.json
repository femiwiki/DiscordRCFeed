{
  "private": true,
  "scripts": {
    "test": "npm -s run lint",
    "lint": "npm -s run lint:prettier",
    "lint:prettier": "prettier . --check",
    "fix": "prettier . --write"
  },
  "devDependencies": {
    "@prettier/plugin-xml": "^3.1.0",
    "prettier": "2.8.7"
  }
}
