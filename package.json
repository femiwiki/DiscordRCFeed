{
  "private": true,
  "scripts": {
    "test": "npm -s run lint",
    "lint": "npm -s run lint:prettier",
    "lint:prettier": "prettier . --check",
    "fix": "prettier . --write"
  },
  "devDependencies": {
    "@prettier/plugin-xml": "^0.13.1",
    "prettier": "^2.3.2"
  }
}
