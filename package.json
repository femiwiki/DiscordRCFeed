{
  "private": true,
  "scripts": {
    "test": "npm -s run lint",
    "lint": "npm -s run lint:prettier",
    "lint:prettier": "prettier . --check",
    "fix": "prettier . --write"
  },
  "devDependencies": {
    "@prettier/plugin-xml": "^1.0.2",
    "prettier": "2.4.0"
  }
}
